# Netlify configuration file

[build]
  # No build command needed for static site
  publish = "."

# Redirect rules - serve index.html as the main page
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
